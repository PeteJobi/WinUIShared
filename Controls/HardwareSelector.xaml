<UserControl
    x:Class="WinUIShared.Controls.HardwareSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    x:Name="Root"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:CollectionVisibilityConverter x:Name="CollectionVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Visibility="{x:Bind viewModel.Gpus, Mode=OneWay, Converter={StaticResource CollectionVisibilityConverter}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <CheckBox Name="UseHardwareAccel" Checked="OnChecked" Unchecked="OnUnchecked">
            <TextBlock TextTrimming="CharacterEllipsis">Hardware acceleration</TextBlock>
        </CheckBox>
        <ComboBox Grid.Column="1" ItemsSource="{x:Bind viewModel.Gpus, Mode=OneWay}" HorizontalAlignment="Center" Margin="10,0,0,0"
                  SelectedItem="{x:Bind SelectedGpu, Mode=TwoWay}" Visibility="{x:Bind UseHardwareAccel.IsChecked, Mode=OneWay}"/>
    </Grid>

</UserControl>