<UserControl
    x:Class="WinUIShared.Controls.HardwareSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:WinUIShared.Controls"
    x:Name="Root"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <CheckBox Name="UseHardwareAccel" Checked="OnChecked" Unchecked="OnUnchecked">Hardware acceleration</CheckBox>
        <ComboBox Grid.Column="1" ItemsSource="{x:Bind viewModel.GPUs, Mode=OneWay}" HorizontalAlignment="Center"
                  SelectedItem="{x:Bind SelectedGpu, Mode=TwoWay}" Visibility="{x:Bind UseHardwareAccel.IsChecked, Mode=OneWay}"/>
    </Grid>

</UserControl>